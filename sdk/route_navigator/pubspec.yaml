name: route_navigator
description: Unicode apps route navigation solution
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  needle:
    path: ../needle

  monitoring:
    path: ../monitoring

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
